# Sveltia CMS config — The Flavor House (lightweight/stopgap version)
#
# Backend: GitHub, via our own OAuth proxy at netlify/functions/auth.js
# (env vars OAUTH_CLIENT_ID / OAUTH_CLIENT_SECRET must be set on the Netlify project).
# Editor account must have write access to theflavorhouse130/flavor-house.
#
# base_url / site_url / display_url below point at the live Netlify subdomain
# (zingy-sprite-a1e2eb.netlify.app), deliberately NOT flavorhouse130.com. Register the
# GitHub OAuth App's callback against this same subdomain — its SSL cert is guaranteed
# live, whereas the custom domain's cert may still be provisioning. Add the custom
# domain as a second OAuth callback URL later once its HTTPS is confirmed working.
#
# ⚠️ This is the STOPGAP CMS: one raw-HTML field for the whole page, not per-section
# fields. A bad edit here can break the page. The proper version (matching Stanford
# Electric / Fête — structured JSON, field-by-field editing) is still a to-do, not
# done. Don't mistake this config for that.

backend:
  name: github
  repo: theflavorhouse130/flavor-house
  branch: main
  base_url: https://zingy-sprite-a1e2eb.netlify.app
  auth_endpoint: .netlify/functions/auth

media_folder: assets/img
public_folder: assets/img

site_url: https://zingy-sprite-a1e2eb.netlify.app
display_url: https://zingy-sprite-a1e2eb.netlify.app
logo_url: https://zingy-sprite-a1e2eb.netlify.app/assets/img/fh-monogram.jpg

collections:
  - name: pages
    label: Website
    files:
      - name: home
        label: Homepage (raw HTML — edit with care)
        file: index.html
        fields:
          - name: body
            label: Full page HTML
            widget: code
            default_language: html
            allow_language_selection: false
            output_code_only: true
